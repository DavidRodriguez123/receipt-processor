package com.interview.receipt_processor.service;

import com.interview.receipt_processor.entity.Receipt;

public interface IReceiptService {
    Receipt createPointsId(Receipt receipt);
    Receipt getPointsById(Long id);
}
